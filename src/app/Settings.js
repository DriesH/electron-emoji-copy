import React, { PureComponent } from 'react';

class Settings extends PureComponent {
  render() {
    return (
      <div>
        <h1>Settings</h1>
      </div>
    );
  }
}

export default Settings;
